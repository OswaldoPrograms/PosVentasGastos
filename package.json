{
  "name": "posventas-gastos",
  "version": "1.0.0",
  "description": "Generador Reporte STUCV",
  "main": "index.html",
  "scripts": {
    "build": "cp index.html www/index.html",
    "sync": "npm run build && cap sync",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  }
}
